{
    "name": "merge-sort",
    "version": "1.0.0",
    "description": "Merge Sort implementation in TypeScript with unit tests",
    "scripts": {
        "test": "jest"
    },
    "devDependencies": {
        "@types/jest": "^27.5.2",
        "jest": "^27.4.7",
        "ts-jest": "^27.1.3",
        "typescript": "^4.5.5"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node"
    }
}
